openapi: 3.0.0
info:
  title: API Ping
  version: 1.0.0
servers:
  - url: http://localhost:8000
paths:
  /ping:
    get:
      responses:
        200:
          description: pong
          content:
            text/plain:
              schema:
                type: string
                example: pong